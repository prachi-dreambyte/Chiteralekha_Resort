import GalleryPage from '@/pages/Gallery/GalleryPage';

export const metadata = {
  title: 'Gallery | Chitralekha Boutique Resort',
  description: 'Explore our gallery of rooms, events, weddings, and celebrations.',
};

export default function Gallery() {
  return <GalleryPage />;
}
